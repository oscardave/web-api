---
description: 常驻哨兵——三闸门纪律、架构约定、编码红线、反模式拦截
globs:
alwaysApply: true
---

# Sentinel · PHP/Hyperf 常驻哨兵规则

## 1. 三闸门执行纪律

每次任务 **必须** 按顺序通过三道闸门，不可跳过、不可合并。

| 阶段 | 命令 | 时机 |
| ---- | ---- | ---- |
| 入场 | `bash scripts/ai-gatekeeper.sh` | 对话首轮 |
| 规划 | `bash scripts/ai-plan.sh [S\|M\|L]` | 首次写代码前 |
| 交付 | `bash scripts/ai-deliver.sh` | 代码修改完成后 |

**变更规模**：S（≤2 文件）｜M（多文件 / 跨 Dao）｜L（新模块 / 新 Controller / 跨层级）

脚本 `exit 1` 即阻断，修复后重新运行，不得绕过。

**可选闸门**（核心代码变更时推荐）：

| 阶段 | 命令 | 时机 |
| ---- | ---- | ---- |
| 知识同步 | `bash scripts/ai-knowledge-sync.sh [--warn-only]` | 核心代码变更 + 交付前 |

S 级 `--warn-only`；M/L 级硬阻断。

## 2. 知识路由

| 内容 | 文件 |
| ---- | ---- |
| 架构约定与编码规范 | `docs/engineering-rules.md` |
| 现有代码模式参考 | `docs/code-patterns.md` |
| 历史踩坑记录 | `docs/ai-lessons-learned.md` |
| 规则加载索引（SSOT） | `docs/rules-index.md` |
| Agent 入口与知识导航 | `AGENTS.md` |
| 业务术语词典 | `knowledge/business-glossary.md` |
| 已冻结决策 | `memory/decisions.md` |

**触发加载**：涉及 WebSocket → `docs/前端WebSocket账变推送对接文档.md` ｜ 涉及 Synapse → `knowledge/01-项目总览与架构.md` ｜ 涉及新 Model → `knowledge/04-数据库模型说明.md`

## 3. 架构约定

### 三层架构

Controller → Dao → DB / External（Service 为可选跨域层）

> 目录映射详见 `docs/engineering-rules.md` §2

### 两套 JSON 信封（严禁混用）

| 域 | 路由前缀 | 基类 | 成功（带数据） | 失败 | 成功（纯消息） |
| -- | -------- | ---- | -------------- | ---- | -------------- |
| Backend | `/ht/v1/` | `BackendController` | `$this->responseData($d)` | `$this->responseError($m)` | `$this->responseSuccess($m)` |
| Frontend | `api/v1/`（无前导 `/`） | `FrontendController` + `BaseJsonTrait` | `self::jsonResult($d)` | `self::jsonErr($m)` | `self::jsonOk($m)` |

> 信封格式与示例详见 `docs/engineering-rules.md` §3

### Dao 规范

静态方法，返回 `['error' => string, 'data' => ...]`，`error` 空字符串 `''` = 成功。

### 依赖方向

- Controller → Dao / Validation / Service（**单向**，禁止反向）
- Backend / Frontend / Internal 三域**互斥**：不可跨域引用 Controller 或 Dao

## 4. 编码红线

- `declare(strict_types=1)` **必须**出现在每个 PHP 文件
- 禁止 `var_dump` / `print_r` / `echo` / `dd` / `dump` 出现在非 `scripts/` 目录
- 禁止注释掉的调试代码（如 `// print_r(...)` `// echo ...`）
- 新方法**必须**有参数类型和返回类型声明
- 禁止在 Controller 中直接写 SQL 查询（SQL 属于 Dao 层）
- 禁止 Swoole 全局可变状态（`global` 变量、类静态可变属性）——协程间会互相污染
- 禁止空 Validation 存根——要么实现完整校验逻辑，要么不创建 Validation 类

## 5. 反模式拦截

命中以下**任一**即刻停止，报告违规编号（如 `AP-3`），不继续生成代码：

| 编号 | 反模式 | 判定条件 |
| ---- | ------ | -------- |
| AP-1 | 跳过闸门 | 未执行入场 / 规划 / 交付脚本就开始或完成任务 |
| AP-2 | 篡改变更级别 | 实际变更范围超过声明的 S/M/L 级别 |
| AP-3 | 信封混用 | Frontend 用 `responseData/responseError/responseSuccess`，或 Backend 用 `jsonResult/jsonErr/jsonOk` |
| AP-4 | 跨域引用 | Backend Controller 引用 `Frontend\Dao\*`（或反向） |
| AP-5 | Controller 写 SQL | Controller 方法体内出现 `Db::` 调用 |
| AP-6 | Dao 返回格式错误 | Dao 方法未返回 `['error' => string, 'data' => ...]` 标准格式 |
| AP-7 | Swoole 全局状态 | 使用 `global` 或类静态可变属性存储请求级数据 |
