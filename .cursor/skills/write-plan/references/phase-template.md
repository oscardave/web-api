# Phase XX: <Name>

> Complexity: **S** | Files: ≤ 2 | Tasks: ≤ 5 | Est: ~X min

## Objective

- One sentence: what this phase achieves.

## Session Context

> Everything a fresh `/clear` window needs to start working.

**Files to open:**
- `<exact/path/to/file-1>` — <what this file is>
- `<exact/path/to/file-2>` — <what this file is>

**Current state of the code:**
```js
// Key snippet showing what the code looks like BEFORE this phase starts.
// Include enough context (~10–20 lines) so the executor can locate the right spot.
```

**What prior phases changed (if any):**
- Phase XX added `SOME_CONSTANT` at ~L780 of file-1 (used in Task 2 below).
- No other dependencies.

## Tasks

### Task 1: <Verb + Object>

**File**: `<path>` ~L<line range>

**Before**:
```js
// exact current code
```

**After**:
```js
// exact target code
```

**Verify**: <how to confirm this task is done>

---

### Task 2: <Verb + Object>

**File**: `<path>` ~L<line range>

**Before**:
```js
// exact current code
```

**After**:
```js
// exact target code
```

**Verify**: <how to confirm this task is done>

## Verification

- Commands:
  - `<concrete runnable command>`
- Manual check:
  - <observable behavior to confirm>

## Exit Criteria

- [ ] <Observable, testable condition 1>
- [ ] <Observable, testable condition 2>
